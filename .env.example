# Skippr Container Environment Variables
# Set these in Coolify when deploying Skippr

# Optional: Anthropic API key for Claude Code
# If not set, you can authenticate via browser when running claude-code
# This uses your Claude.ai Pro plan credits instead of API credits
# Only set this if you want to use API tokens instead of web auth
# Get API key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Optional: Set your timezone (defaults to UTC)
# Examples: America/New_York, Europe/London, Asia/Tokyo
TZ=UTC
